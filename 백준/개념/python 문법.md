# 1. 함수를 변수에 담기 + 덮어쓰기
- 원래 있던 input()을 덮어쓰고, 더 빠른 입력 함수를 쓰도록 연결

# 2. 리스트 컴프리헨션

# 3. 리스트 안의 리스트 (이차원배열) or 리스트 안의 튜플
 a, b = 리스트이름[idx] 
 => 리스트 안의 리스트/튜플속 숫자쌍을 각각 꺼내어 변수로 담을 수있음
 => 리스트 안의 리스트는 수정가능 / 튜플은 수정 불가능
  
 # 4. if __name__ == "__main__":
    if __name__ == "__main__":
    solve()
    이 파일을 직접 실행할 때만 아래 코드를 실행하라”**는 뜻이에요.

- 내가 직접 python myfile.py로 실행하면 → __name__ == "__main__" 참 → solve() 실행
- 다른 파일에서 import myfile 하면 → __name__ == "myfile" → 조건이 거짓 → solve() 실행 안 됨

